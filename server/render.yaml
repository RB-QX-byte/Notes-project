# Render Configuration for Notes App Backend
# This file tells Render how to build and deploy the server

# Build settings
buildCommand = "npm install && npm rebuild better-sqlite3"
startCommand = "npm start"

# Environment
[env]
  NODE_VERSION = "20"
  NPM_CONFIG_PRODUCTION = "false"
